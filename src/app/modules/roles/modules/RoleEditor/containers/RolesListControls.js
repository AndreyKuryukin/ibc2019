import { connect } from 'react-redux';
import Controls from '../components/RolesListControls/RolesListControls';

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

const RolesListControls = connect(
    mapStateToProps,
    mapDispatchToProps,
)(Controls);

export default RolesListControls;
